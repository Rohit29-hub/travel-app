import { Inter } from "next/font/google";
import { Newsletter } from "@/components/layout/Newsletter";
import "./globals.css";

import { Navbar } from "@/components/layout/Navbar";
import { Footer } from "@/components/layout/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "TravelHub | Top level Services Let's Travel with us",
  description: "Generated by create next app",
};

export default function RootLayout({ children, modals }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        {modals}
        {children}
        <Newsletter category={"subscribe to our newsletter"} title={"Prepare yourself & let’s explore the beauty of the world"}/> 
        <Footer />
      </body>
    </html>
  );
}
